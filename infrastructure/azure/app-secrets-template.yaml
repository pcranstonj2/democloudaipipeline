apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: transaction-pipeline
type: Opaque
stringData:
  # Add only sensitive values here and remove placeholders before applying.
  # Example values that may become sensitive as the platform evolves:
  MODEL_API_KEY: "replace-me"
  KAFKA_USERNAME: "replace-me"
  KAFKA_PASSWORD: "replace-me"
